model:
  run_name: "Provolone"
  name: "QNN1"
  load: False # False or the name of the model
  save: True # for overwriting

data:
  data_path: "data/data_01_15.pgn"

pretraining:
  enabled: False
  batch_size: 256
  epochs: 3
  learning_rate: 0.001

training:
  enabled: True
  episodes: 100
  batch_size: 32
